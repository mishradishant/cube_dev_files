cubes:
  - name: pillar
    sql_table: Dim.Pillar
    data_source: default

    joins: []

    dimensions:
      - name: PillarKey
        sql: PillarKey
        type: number
        primary_key: true

      - name: ClientPillar
        sql: ClientPillar
        type: string

      - name: RevenuePillar
        sql: RevenuePillar
        type: string

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

