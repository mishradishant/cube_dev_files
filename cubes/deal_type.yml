cubes:
  - name: deal_type
    sql_table: Dim.DealType
    data_source: default

    joins: []

    dimensions:
      - name: DealTypeKey
        sql: DealTypeKey
        type: number
        primary_key: true

      - name: DealTypeDesc
        sql: DealTypeDesc
        type: string


    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started