cubes:
  - name: currency_type
    sql_table: DIM.vw_CurrencyType
    data_source: default

    joins: []

    dimensions:
      - name: CurrencyTypeKey
        sql: CurrencyTypeKey
        type: number
        primary_key: true

      - name: CurrencyType
        sql: CurrencyType
        type: string


    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

