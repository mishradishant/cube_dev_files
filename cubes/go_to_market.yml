cubes:
  - name: go_to_market
    sql: SELECT DISTINCT GTM FROM Fact.ClientKpi AS FC
    data_source: default

    joins: []

    dimensions:
      - name: GTM
        sql: GTM
        type: string
        primary_key: true
  


    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started