cubes:
  - name: bands_for_turnover
    sql_table: Dim.vw_BandsForTurnover
    #data_source: default

    #joins: []

    dimensions:
      - name: BandId
        sql: BandId
        type: number
        primary_key: true

      - name: TurnoverBands
        sql: BandRange
        type: string


    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

