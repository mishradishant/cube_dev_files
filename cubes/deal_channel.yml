cubes:
  - name: deal_channel
    sql_table: Dim.DealChannel
    data_source: default

    joins: []

    dimensions:
      - name: dealchannelkey
        sql: dealchannelkey
        type: number
        primary_key: true
    
      - name: dealchannel
        sql: dealchannelParent
        type: string

      - name: dealtbychannel
        sql: dealchannel
        type: string
    

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

