cubes:
  - name: atlas_transaction
    sql_table: dev_curated.atlas_transaction
    data_source: default

    joins: []

    dimensions:
      - name: system_id
        sql: system_id
        type: string

      - name: organization_code
        sql: organization_code
        type: string

      - name: organization_name
        sql: organization_name
        type: string

      - name: account_name
        sql: account_name
        type: string

      - name: crm_account_id
        sql: crm_account_id
        type: string

      - name: titan_account_number
        sql: titan_account_number
        type: string

      - name: contact_name
        sql: contact_name
        type: string

      - name: instruction_number
        sql: instruction_number
        type: string

      - name: created_by_sso_user_id
        sql: created_by_sso_user_id
        type: string

      - name: created_on
        sql: created_on
        type: string

      - name: updated_by_sso_user_id
        sql: updated_by_sso_user_id
        type: string

      - name: updated_on
        sql: updated_on
        type: string

      - name: transaction_date
        sql: transaction_date
        type: string

      - name: currency
        sql: currency
        type: string

      - name: compliance_status
        sql: compliance_status
        type: string

      - name: compliance_done_on
        sql: compliance_done_on
        type: string

      - name: fraugster_status
        sql: fraugster_status
        type: string

      - name: sanction_status
        sql: sanction_status
        type: string

      - name: blacklist_status
        sql: blacklist_status
        type: string

      - name: custom_check_status
        sql: custom_check_status
        type: string

      - name: legacy_trade_contract_number
        sql: legacy_trade_contract_number
        type: string

      - name: legal_entity_code
        sql: legal_entity_code
        type: string

      - name: legal_entity_name
        sql: legal_entity_name
        type: string

      - name: initial_status
        sql: initial_status
        type: string

      - name: debit_card_fraud_check_status
        sql: debit_card_fraud_check_status
        type: string

      - name: intuition_client_risk_level
        sql: intuition_client_risk_level
        type: string

      - name: intuition_check_status
        sql: intuition_check_status
        type: string

      - name: payment_reference_status
        sql: payment_reference_status
        type: string

      - name: payment_cleared_on
        sql: payment_cleared_on
        type: string

      - name: description
        sql: description
        type: string

      - name: activity_by_user_id
        sql: activity_by_user_id
        type: string

      - name: activity_timestamp
        sql: activity_timestamp
        type: string

      - name: activity_comments
        sql: activity_comments
        type: string

      - name: activity_created_by_user_id
        sql: activity_created_by_user_id
        type: string

      - name: activity_created_on
        sql: activity_created_on
        type: string

      - name: activity_type_module
        sql: activity_type_module
        type: string

      - name: activity_type_value
        sql: activity_type_value
        type: string

      - name: activity_details_created_on
        sql: activity_details_created_on
        type: string

      - name: activity_log
        sql: activity_log
        type: string

      - name: confirmation_id
        sql: confirmation_id
        type: string

      - name: intraday_statement_line_id
        sql: intraday_statement_line_id
        type: string

      - name: bank_reference
        sql: bank_reference
        type: string

      - name: payment_reference
        sql: payment_reference
        type: string

      - name: iso_country_code
        sql: iso_country_code
        type: string

      - name: query
        sql: query
        type: string

      - name: payment_type
        sql: payment_type
        type: string

      - name: client_account_key
        sql: client_account_key
        type: string

      - name: master_statement_line_id
        sql: master_statement_line_id
        type: string

      - name: source
        sql: source
        type: string

      - name: error_description
        sql: error_description
        type: string

      - name: message
        sql: message
        type: string

      - name: message_no
        sql: message_no
        type: string

      - name: receiver_bic
        sql: receiver_bic
        type: string

      - name: msg_type
        sql: msg_type
        type: string

      - name: transaction_reference
        sql: transaction_reference
        type: string

      - name: iban
        sql: iban
        type: string

      - name: error_code
        sql: error_code
        type: string

      - name: status
        sql: status
        type: string

      - name: reason
        sql: reason
        type: string

      - name: account_identification
        sql: account_identification
        type: string

      - name: remitter_account_name
        sql: remitter_account_name
        type: string

      - name: remitter_account_number
        sql: remitter_account_number
        type: string

      - name: remitter_address
        sql: remitter_address
        type: string

      - name: remitter_region
        sql: remitter_region
        type: string

      - name: remitter_info
        sql: remitter_info
        type: string

      - name: remitter_type
        sql: remitter_type
        type: string

      - name: cd_bank_code
        sql: cd_bank_code
        type: string

      - name: cd_bank_name
        sql: cd_bank_name
        type: string

      - name: cd_bank_address
        sql: cd_bank_address
        type: string

      - name: cd_bank_country
        sql: cd_bank_country
        type: string

      - name: cd_bank_bic
        sql: cd_bank_bic
        type: string

      - name: rof_money
        sql: rof_money
        type: string

      - name: rof_balance
        sql: rof_balance
        type: string

      - name: rof_classification
        sql: rof_classification
        type: string

      - name: is_nak_response
        sql: is_nak_response
        type: string

      - name: nak_banking_portal_request
        sql: nak_banking_portal_request
        type: string

      - name: rof_description
        sql: rof_description
        type: string

      - name: credit_advice_id
        sql: credit_advice_id
        type: string

      - name: statement_line_id
        sql: statement_line_id
        type: string

      - name: statement_date
        sql: statement_date
        type: string

      - name: entry_date
        sql: entry_date
        type: string

      - name: debit_credit_code
        sql: debit_credit_code
        type: string

      - name: transaction_code
        sql: transaction_code
        type: string

      - name: alt_bank_reference
        sql: alt_bank_reference
        type: string

      - name: counterparty_reference
        sql: counterparty_reference
        type: string

      - name: client_reference
        sql: client_reference
        type: string

      - name: reference
        sql: reference
        type: string

      - name: account_info
        sql: account_info
        type: string

      - name: account_identifier
        sql: account_identifier
        type: string

      - name: orginator_bank_swift
        sql: orginator_bank_swift
        type: string

      - name: intermediary_bank_swift
        sql: intermediary_bank_swift
        type: string

      - name: originator_iso_country_code
        sql: originator_iso_country_code
        type: string

      - name: funds_in_info
        sql: funds_in_info
        type: string

      - name: phub_upload_detail_flag
        sql: phub_upload_detail_flag
        type: string

      - name: is_update_eligible
        sql: is_update_eligible
        type: string

      - name: titan_response
        sql: titan_response
        type: string

      - name: is_rof
        sql: is_rof
        type: string

      - name: transaction_method
        sql: transaction_method
        type: string

      - name: pdq_approved_by_user_name
        sql: pdq_approved_by_user_name
        type: string

      - name: titan_ref_id
        sql: titan_ref_id
        type: string

      - name: osr_id
        sql: osr_id
        type: string

      - name: source_of_application
        sql: source_of_application
        type: string

      - name: fund_type
        sql: fund_type
        type: string

      - name: entry_type
        sql: entry_type
        type: string

      - name: payment_instruction_id
        sql: payment_instruction_id
        type: string

      - name: is_processed
        sql: is_processed
        type: string

      - name: is_priority
        sql: is_priority
        type: string

      - name: channel
        sql: channel
        type: string

      - name: client_iso_country_code
        sql: client_iso_country_code
        type: string

      - name: beneficiary
        sql: beneficiary
        type: string

      - name: charges
        sql: charges
        type: string

      - name: client_account_name
        sql: client_account_name
        type: string

      - name: sla_exceed
        sql: sla_exceed
        type: string

      - name: pain_status
        sql: pain_status
        type: string

      - name: intermediary_status
        sql: intermediary_status
        type: string

      - name: swift_code
        sql: swift_code
        type: string

      - name: proprietary_payment_reference
        sql: proprietary_payment_reference
        type: string

      - name: is_reroute_eligible
        sql: is_reroute_eligible
        type: string

      - name: payment_splitted
        sql: payment_splitted
        type: string

      - name: is_reversal_transaction
        sql: is_reversal_transaction
        type: string

      - name: direction
        sql: direction
        type: string

      - name: senders_reference
        sql: senders_reference
        type: string

      - name: bank_operation_code
        sql: bank_operation_code
        type: string

      - name: instruction_code
        sql: instruction_code
        type: string

      - name: transaction_type_code
        sql: transaction_type_code
        type: string

      - name: inter_bank_currency
        sql: inter_bank_currency
        type: string

      - name: instructed_currency
        sql: instructed_currency
        type: string

      - name: ordering_customer
        sql: ordering_customer
        type: string

      - name: ordering_institution
        sql: ordering_institution
        type: string

      - name: senders_correspondent
        sql: senders_correspondent
        type: string

      - name: receivers_correspondent
        sql: receivers_correspondent
        type: string

      - name: third_reimbursement_institution
        sql: third_reimbursement_institution
        type: string

      - name: intermediary_institution
        sql: intermediary_institution
        type: string

      - name: account_with_institution
        sql: account_with_institution
        type: string

      - name: remittance_information
        sql: remittance_information
        type: string

      - name: details_of_charges
        sql: details_of_charges
        type: string

      - name: sender_to_reciever_information
        sql: sender_to_reciever_information
        type: string

      - name: regulatory_reporting
        sql: regulatory_reporting
        type: string

      - name: ack_nak_timestamp
        sql: ack_nak_timestamp
        type: string

      - name: received_date
        sql: received_date
        type: string

      - name: ordering_customer_iso_country_code
        sql: ordering_customer_iso_country_code
        type: string

      - name: beneficiary_type
        sql: beneficiary_type
        type: string

      - name: beneficiary_phone_no
        sql: beneficiary_phone_no
        type: string

      - name: beneficiary_email
        sql: beneficiary_email
        type: string

      - name: purpose_of_payment
        sql: purpose_of_payment
        type: string

      - name: ordering_institution_iso_country_code
        sql: ordering_institution_iso_country_code
        type: string

      - name: is_single_payment
        sql: is_single_payment
        type: string

      - name: queue_name
        sql: queue_name
        type: string

      - name: sent
        sql: sent
        type: string

      - name: is_sent
        sql: is_sent
        type: string

      - name: related_reference
        sql: related_reference
        type: string

      - name: is_ready
        sql: is_ready
        type: string

      - name: payment_out_date
        sql: payment_out_date
        type: string

      - name: fx_ticket_details
        sql: fx_ticket_details
        type: string

      - name: business_partner
        sql: business_partner
        type: string

      - name: receiver_city
        sql: receiver_city
        type: string

      - name: receiver_state_province
        sql: receiver_state_province
        type: string

      - name: receiver_postal_code
        sql: receiver_postal_code
        type: string

      - name: bank_query_reference
        sql: bank_query_reference
        type: string

      - name: last_correspondence
        sql: last_correspondence
        type: string

      - name: is_imported
        sql: is_imported
        type: string

      - name: action_taken_on_query
        sql: action_taken_on_query
        type: string

      - name: is_authorise
        sql: is_authorise
        type: string

      - name: query_type
        sql: query_type
        type: string

      - name: narrative
        sql: narrative
        type: string

      - name: query_state
        sql: query_state
        type: string

      - name: event_id
        sql: event_id
        type: string

      - name: cd_customer_ref
        sql: cd_customer_ref
        type: string

      - name: cd_payment_lifecycle_id
        sql: cd_payment_lifecycle_id
        type: string

      - name: amount_to_block_currency
        sql: amount_to_block_currency
        type: string

      - name: merchant_name
        sql: merchant_name
        type: string

      - name: merchant_address
        sql: merchant_address
        type: string

      - name: transaction_type
        sql: transaction_type
        type: string

      - name: purchase_category
        sql: purchase_category
        type: string

      - name: transaction_description
        sql: transaction_description
        type: string

      - name: crossborder_fee_currency
        sql: crossborder_fee_currency
        type: string

      - name: atm_fee_currency
        sql: atm_fee_currency
        type: string

      - name: transaction_details_currency
        sql: transaction_details_currency
        type: string

      - name: billing_details_currency
        sql: billing_details_currency
        type: string

      - name: transaction_category
        sql: transaction_category
        type: string

      - name: public_token
        sql: public_token
        type: string

      - name: card_present
        sql: card_present
        type: string

      - name: card_present_desc
        sql: card_present_desc
        type: string

      - name: gps_pos_data
        sql: gps_pos_data
        type: string

      - name: merch_country
        sql: merch_country
        type: string

      - name: merch_postcode
        sql: merch_postcode
        type: string

      - name: merch_region
        sql: merch_region
        type: string

      - name: merch_city
        sql: merch_city
        type: string

      - name: authentication_method
        sql: authentication_method
        type: string

      - name: authentication_method_desc
        sql: authentication_method_desc
        type: string

      - name: merch_id
        sql: merch_id
        type: string

      - name: merch_contact
        sql: merch_contact
        type: string

      - name: merch_second_contact
        sql: merch_second_contact
        type: string

      - name: merch_website
        sql: merch_website
        type: string

      - name: additional_amount_type
        sql: additional_amount_type
        type: string

      - name: additional_amount_currency
        sql: additional_amount_currency
        type: string

      - name: reason_id
        sql: reason_id
        type: string

      - name: pos_terminal
        sql: pos_terminal
        type: string

      - name: pos_date
        sql: pos_date
        type: string

      - name: pos_time
        sql: pos_time
        type: string

      - name: gpstransaction_id
        sql: gpstransaction_id
        type: string

      - name: product_id
        sql: product_id
        type: string

      - name: acq_datestamp
        sql: acq_datestamp
        type: string

      - name: transaction_country
        sql: transaction_country
        type: string

      - name: pre_auth_multi_part_ind
        sql: pre_auth_multi_part_ind
        type: string

      - name: pre_auth_multi_part_final
        sql: pre_auth_multi_part_final
        type: string

      - name: pre_auth_matching_reference
        sql: pre_auth_matching_reference
        type: string

      - name: merchant_name_other
        sql: merchant_name_other
        type: string

      - name: mcc_code_description
        sql: mcc_code_description
        type: string

      - name: acquirer_id
        sql: acquirer_id
        type: string

      - name: gps_decision_code
        sql: gps_decision_code
        type: string

      - name: gps_decision_code_description
        sql: gps_decision_code_description
        type: string

      - name: gps_decline_description
        sql: gps_decline_description
        type: string

      - name: card_status_code
        sql: card_status_code
        type: string

      - name: gps_stip_flag
        sql: gps_stip_flag
        type: string

      - name: avs_result
        sql: avs_result
        type: string

      - name: gps_pos_capability
        sql: gps_pos_capability
        type: string

      - name: card_token
        sql: card_token
        type: string

      - name: bin
        sql: bin
        type: string

      - name: ip_address
        sql: ip_address
        type: string

      - name: device_id
        sql: device_id
        type: string

      - name: is_allowed_partial_block
        sql: is_allowed_partial_block
        type: string

      - name: transaction_currency
        sql: transaction_currency
        type: string

      - name: decline_code
        sql: decline_code
        type: string

      - name: decline_reason
        sql: decline_reason
        type: string

      - name: amount_to_credit_currency
        sql: amount_to_credit_currency
        type: string

      - name: amount_to_unblock_currency
        sql: amount_to_unblock_currency
        type: string

      - name: bill_money_currency
        sql: bill_money_currency
        type: string

      - name: master_card_settlement_date
        sql: master_card_settlement_date
        type: string

      - name: interchange_fee_currency
        sql: interchange_fee_currency
        type: string

      - name: interchange_direction
        sql: interchange_direction
        type: string

      - name: amount_to_debit_currency
        sql: amount_to_debit_currency
        type: string

      - name: transaction_money_currency
        sql: transaction_money_currency
        type: string

      - name: payment_token_wallet
        sql: payment_token_wallet
        type: string

      - name: status_id
        sql: status_id
        type: string

      - name: currency_code
        sql: currency_code
        type: string

      - name: status_enum
        sql: status_enum
        type: string

      - name: purpose_code
        sql: purpose_code
        type: string

      - name: priority_payment
        sql: priority_payment
        type: string

      - name: collection_account
        sql: collection_account
        type: string

      - name: third_party
        sql: third_party
        type: string

      - name: payment_method_code
        sql: payment_method_code
        type: string

      - name: funds_moved_to_wallet_on
        sql: funds_moved_to_wallet_on
        type: string

      - name: notify_to_bank
        sql: notify_to_bank
        type: string

      - name: transfer_reason_code
        sql: transfer_reason_code
        type: string

      - name: accept_or_reject
        sql: accept_or_reject
        type: string

      - name: debit_entry_reason_code
        sql: debit_entry_reason_code
        type: string

      - name: bank_legal_entity_code
        sql: bank_legal_entity_code
        type: string

      - name: classification_type
        sql: classification_type
        type: string

      - name: deposit_money
        sql: deposit_money
        type: string

      - name: classification_status_description
        sql: classification_status_description
        type: string

      - name: payment_referencetus
        sql: payment_referencetus
        type: string

      - name: note
        sql: note
        type: string

      - name: banking_payment_method
        sql: banking_payment_method
        type: string

      - name: debtor_act_legal_entries
        sql: debtor_act_legal_entries
        type: string

      - name: creditor_act_legal_entries
        sql: creditor_act_legal_entries
        type: string

      - name: creditor_name
        sql: creditor_name
        type: string

      - name: creditor_account_number
        sql: creditor_account_number
        type: string

      - name: creditor_address
        sql: creditor_address
        type: string

      - name: creditor_country_code
        sql: creditor_country_code
        type: string

      - name: creditor_bank
        sql: creditor_bank
        type: string

      - name: creditor_bank_bic
        sql: creditor_bank_bic
        type: string

      - name: creditor_intermediary_bank
        sql: creditor_intermediary_bank
        type: string

      - name: debtor_name
        sql: debtor_name
        type: string

      - name: debtor_account_number
        sql: debtor_account_number
        type: string

      - name: debtor_address
        sql: debtor_address
        type: string

      - name: debtor_country_code
        sql: debtor_country_code
        type: string

      - name: debtor_bank
        sql: debtor_bank
        type: string

      - name: debtor_bank_bic
        sql: debtor_bank_bic
        type: string

      - name: debtor_region
        sql: debtor_region
        type: string

      - name: debtor_intermediary_bank
        sql: debtor_intermediary_bank
        type: string

      - name: arm_account_number
        sql: arm_account_number
        type: string

      - name: hint_account_number
        sql: hint_account_number
        type: string

      - name: hint_rules
        sql: hint_rules
        type: string

      - name: allocation_type
        sql: allocation_type
        type: string

      - name: banking_manually_approved_by
        sql: banking_manually_approved_by
        type: string

      - name: notes
        sql: notes
        type: string

      - name: device_info
        sql: device_info
        type: string

      - name: legacy_instruction_number
        sql: legacy_instruction_number
        type: string

      - name: customer_instruction_type_code
        sql: customer_instruction_type_code
        type: string

      - name: instruction_create_date
        sql: instruction_create_date
        type: string

      - name: source_application_code
        sql: source_application_code
        type: string

      - name: external_source_ref_number
        sql: external_source_ref_number
        type: string

      - name: ful_fillment_date
        sql: ful_fillment_date
        type: string

      - name: cancellation_reason_code
        sql: cancellation_reason_code
        type: string

      - name: is_option
        sql: is_option
        type: string

      - name: is_limit_order
        sql: is_limit_order
        type: string

      - name: is_sellback
        sql: is_sellback
        type: string

      - name: fx_action
        sql: fx_action
        type: string

      - name: payment_life_id
        sql: payment_life_id
        type: string

      - name: mbl_transaction_id
        sql: mbl_transaction_id
        type: string

      - name: mbl_deal_created_on
        sql: mbl_deal_created_on
        type: string

      - name: is_manual_deal
        sql: is_manual_deal
        type: string

      - name: mbl_status
        sql: mbl_status
        type: string

      - name: customer_payment_method_code
        sql: customer_payment_method_code
        type: string

      - name: card_brand_code
        sql: card_brand_code
        type: string

      - name: name_on_card
        sql: name_on_card
        type: string

      - name: masked_card_number
        sql: masked_card_number
        type: string

      - name: start_date
        sql: start_date
        type: string

      - name: expiry_date
        sql: expiry_date
        type: string

      - name: card_holder_street
        sql: card_holder_street
        type: string

      - name: card_holder_city
        sql: card_holder_city
        type: string

      - name: card_holder_state
        sql: card_holder_state
        type: string

      - name: card_holder_post_code
        sql: card_holder_post_code
        type: string

      - name: card_holder_country_code
        sql: card_holder_country_code
        type: string

      - name: card_reference
        sql: card_reference
        type: string

      - name: cardhash
        sql: cardhash
        type: string

      - name: token_expiry_date
        sql: token_expiry_date
        type: string

      - name: token_event_reference
        sql: token_event_reference
        type: string

      - name: shopper_id
        sql: shopper_id
        type: string

      - name: use_registered_address
        sql: use_registered_address
        type: string

      - name: default_card
        sql: default_card
        type: string

      - name: can_reuse
        sql: can_reuse
        type: string

      - name: active
        sql: active
        type: string

      - name: legacy_payment_method_code
        sql: legacy_payment_method_code
        type: string

      - name: in_bound_out_bound
        sql: in_bound_out_bound
        type: string

      - name: used_in_payee
        sql: used_in_payee
        type: string

      - name: charge_type
        sql: charge_type
        type: string

      - name: reasons_for_hold_code
        sql: reasons_for_hold_code
        type: string

      - name: bank_op_code
        sql: bank_op_code
        type: string

      - name: sender_bic
        sql: sender_bic
        type: string

      - name: reciever_bic
        sql: reciever_bic
        type: string

      - name: reciever_intra_country_code
        sql: reciever_intra_country_code
        type: string

      - name: payee_reference
        sql: payee_reference
        type: string

      - name: funds_move_to_wallet_on
        sql: funds_move_to_wallet_on
        type: string

      - name: funds_send_to_bank_on
        sql: funds_send_to_bank_on
        type: string

      - name: bank_notification_rcvd_on
        sql: bank_notification_rcvd_on
        type: string

      - name: banking_status_code
        sql: banking_status_code
        type: string

      - name: on_hold
        sql: on_hold
        type: string

      - name: online
        sql: online
        type: string

      - name: estimate_deliver_date
        sql: estimate_deliver_date
        type: string

      - name: payment_execution_date
        sql: payment_execution_date
        type: string

      - name: credit_advice_sent_for_payment
        sql: credit_advice_sent_for_payment
        type: string

      - name: debit_advice_received_from_bank
        sql: debit_advice_received_from_bank
        type: string

      - name: debiting_account_number
        sql: debiting_account_number
        type: string

      - name: etailer_journey_type
        sql: etailer_journey_type
        type: string

      - name: is_rmt_payment_flag
        sql: is_rmt_payment_flag
        type: string

      - name: lock
        sql: lock
        type: string

      - name: transaction_reference_number
        sql: transaction_reference_number
        type: string

      - name: category
        sql: category
        type: string

      - name: ordering_customer_name
        sql: ordering_customer_name
        type: string

      - name: ordering_custome_mailing_street
        sql: ordering_custome_mailing_street
        type: string

      - name: ordering_customer_mailing_city
        sql: ordering_customer_mailing_city
        type: string

      - name: ordering_customer_mailing_state
        sql: ordering_customer_mailing_state
        type: string

      - name: ordering_customer_mailing_country_code
        sql: ordering_customer_mailing_country_code
        type: string

      - name: creditor_local_name
        sql: creditor_local_name
        type: string

      - name: destination_type
        sql: destination_type
        type: string

      - name: payment_date
        sql: payment_date
        type: string

      - name: reason_of_fund_code
        sql: reason_of_fund_code
        type: string

      - name: erroneous
        sql: erroneous
        type: string

      - name: error_reason
        sql: error_reason
        type: string

      - name: deleted
        sql: deleted
        type: string

      - name: base_currency
        sql: base_currency
        type: string

      - name: selling_currency_code
        sql: selling_currency_code
        type: string

      - name: buying_currency_code
        sql: buying_currency_code
        type: string

      - name: book_currency_code
        sql: book_currency_code
        type: string

      - name: reason_for_adjustment_code
        sql: reason_for_adjustment_code
        type: string

      - name: value_date
        sql: value_date
        type: string

      - name: b2_b
        sql: b2_b
        type: string

      - name: b2_b_position
        sql: b2_b_position
        type: string

      - name: sent_to_bank_on
        sql: sent_to_bank_on
        type: string

      - name: affiliate_commission_paid
        sql: affiliate_commission_paid
        type: string

      - name: affiliate_commission_paid_on
        sql: affiliate_commission_paid_on
        type: string

      - name: blotter_category_code
        sql: blotter_category_code
        type: string

      - name: reasons_for_status_code
        sql: reasons_for_status_code
        type: string

      - name: reasons_for_ticket_code
        sql: reasons_for_ticket_code
        type: string

      - name: conflict_status_code
        sql: conflict_status_code
        type: string

      - name: internal
        sql: internal
        type: string

      - name: cancelled_on
        sql: cancelled_on
        type: string

      - name: etailer_flag
        sql: etailer_flag
        type: string

      - name: closed_on
        sql: closed_on
        type: string

      - name: income_source_of_fund_details
        sql: income_source_of_fund_details
        type: string

      - name: fx_type_code
        sql: fx_type_code
        type: string

      - name: conflict_resolved_on
        sql: conflict_resolved_on
        type: string

      - name: intermediary_bank_bic
        sql: intermediary_bank_bic
        type: string

      - name: opi_added_source
        sql: opi_added_source
        type: string

      - name: beneficiary_added_source
        sql: beneficiary_added_source
        type: string

      - name: sub_category
        sql: sub_category
        type: string

      - name: payment_out_state
        sql: payment_out_state
        type: string

      - name: payment_status
        sql: payment_status
        type: string

      - name: payment_instructions_details_id
        sql: payment_instructions_details_id
        type: string

      - name: repeat_beneficiary_status
        sql: repeat_beneficiary_status
        type: string

      - name: creditor_bank_address
        sql: creditor_bank_address
        type: string

      - name: remitter_post_code
        sql: remitter_post_code
        type: string

      - name: country_specific_bank_code
        sql: country_specific_bank_code
        type: string

      - name: country_specific_bank_code_type
        sql: country_specific_bank_code_type
        type: string

      - name: fcc_detail
        sql: fcc_detail
        type: string

      - name: amount_type
        sql: amount_type
        type: string

      - name: cancelled
        sql: cancelled
        type: string

      - name: execute_instruction
        sql: execute_instruction
        type: string

      - name: instruction_type
        sql: instruction_type
        type: string

      - name: limit_hit
        sql: limit_hit
        type: string

      - name: rate_engine_sub_number
        sql: rate_engine_sub_number
        type: string

      - name: reference_number
        sql: reference_number
        type: string

      - name: target_end_date
        sql: target_end_date
        type: string

      - name: target_meet_on
        sql: target_meet_on
        type: string

      - name: target_rate_lower
        sql: target_rate_lower
        type: string

      - name: card_request_type_code
        sql: card_request_type_code
        type: string

      - name: is_multi_currency
        sql: is_multi_currency
        type: string

      - name: requested_currency_code
        sql: requested_currency_code
        type: string

      - name: cross_border_fee_description
        sql: cross_border_fee_description
        type: string

      - name: atm_fee_description
        sql: atm_fee_description
        type: string

      - name: master_card_fee_description
        sql: master_card_fee_description
        type: string

      - name: master_card_fee_currency_code
        sql: master_card_fee_currency_code
        type: string

      - name: is_card_spent
        sql: is_card_spent
        type: string

      - name: mcc_code
        sql: mcc_code
        type: string

      - name: card_input_mode
        sql: card_input_mode
        type: string

      - name: card_input_mode_desc
        sql: card_input_mode_desc
        type: string

      - name: merch_street
        sql: merch_street
        type: string

      - name: reason_id_description
        sql: reason_id_description
        type: string

      - name: cheque_date
        sql: cheque_date
        type: string

      - name: cheque_number
        sql: cheque_number
        type: string

      - name: bank_city
        sql: bank_city
        type: string

      - name: customer_wallet_number
        sql: customer_wallet_number
        type: string

      - name: customer_wallet_currency_code
        sql: customer_wallet_currency_code
        type: string

      - name: org_wallet_type_code
        sql: org_wallet_type_code
        type: string

      - name: org_wallet_number
        sql: org_wallet_number
        type: string

      - name: org_wallet_currency_code
        sql: org_wallet_currency_code
        type: string

      - name: org_wallet_wallet_number
        sql: org_wallet_wallet_number
        type: string

      - name: maturity_date
        sql: maturity_date
        type: string

      - name: open_date
        sql: open_date
        type: string

      - name: open_forward
        sql: open_forward
        type: string

      - name: entire_amount_released
        sql: entire_amount_released
        type: string

      - name: amount_released_on
        sql: amount_released_on
        type: string

      - name: event_type
        sql: event_type
        type: string

      - name: event_date
        sql: event_date
        type: string

      - name: event_status
        sql: event_status
        type: string

      - name: event_reason
        sql: event_reason
        type: string

      - name: billing_currency_code
        sql: billing_currency_code
        type: string

      - name: fee_currency_code
        sql: fee_currency_code
        type: string

      - name: is_payment_fees
        sql: is_payment_fees
        type: string

      - name: fee_charged_on
        sql: fee_charged_on
        type: string

      - name: due_date
        sql: due_date
        type: string

      - name: fee_updated_on_wallet
        sql: fee_updated_on_wallet
        type: string

      - name: notification_sent_to_bank
        sql: notification_sent_to_bank
        type: string

      - name: payee_type
        sql: payee_type
        type: string

      - name: intermediary_bank_intra_country_code
        sql: intermediary_bank_intra_country_code
        type: string

      - name: intermediary_bank_intra_country_code_type
        sql: intermediary_bank_intra_country_code_type
        type: string

      - name: intermediary_bank_name
        sql: intermediary_bank_name
        type: string

      - name: intermediary_bank_street
        sql: intermediary_bank_street
        type: string

      - name: intermediary_bank_city
        sql: intermediary_bank_city
        type: string

      - name: intermediary_bank_state
        sql: intermediary_bank_state
        type: string

      - name: intermediary_bank_post_code
        sql: intermediary_bank_post_code
        type: string

      - name: intermediary_bank_country_name
        sql: intermediary_bank_country_name
        type: string

      - name: dd_activation_date
        sql: dd_activation_date
        type: string

      - name: dd_reference
        sql: dd_reference
        type: string

      - name: direct_debit
        sql: direct_debit
        type: string

      - name: memo
        sql: memo
        type: string

      - name: other_reference
        sql: other_reference
        type: string

      - name: filename
        sql: filename
        type: string

      - name: message_header_cd_id
        sql: message_header_cd_id
        type: string

      - name: message_header_transaction_sequence_id
        sql: message_header_transaction_sequence_id
        type: string

      - name: message_header_transaction_date_time
        sql: message_header_transaction_date_time
        type: string

      - name: message_header_source
        sql: message_header_source
        type: string

      - name: message_header_operation
        sql: message_header_operation
        type: string

      - name: message_header_transaction_name
        sql: message_header_transaction_name
        type: string

      - name: year
        sql: year
        type: string

      - name: month
        sql: month
        type: string

      - name: day
        sql: day
        type: string

    measures:
      - name: count
        type: count

      - name: parent_account_number
        sql: parent_account_number
        type: sum

      - name: amount
        sql: amount
        type: sum

      - name: virtual_account_number
        sql: virtual_account_number
        type: sum

      - name: auth_user_count
        sql: auth_user_count
        type: sum

      - name: charge_amount
        sql: charge_amount
        type: sum

      - name: inter_bank_amount
        sql: inter_bank_amount
        type: sum

      - name: instructed_amount
        sql: instructed_amount
        type: sum

      - name: transaction_details_amount
        sql: transaction_details_amount
        type: sum

      - name: billing_details_amount
        sql: billing_details_amount
        type: sum

      - name: additional_amount
        sql: additional_amount
        type: sum

      - name: base_currency_amount
        sql: base_currency_amount
        type: sum

      - name: selling_amount
        sql: selling_amount
        type: sum

      - name: buying_amount
        sql: buying_amount
        type: sum

      - name: base_selling_amount
        sql: base_selling_amount
        type: sum

      - name: base_buying_amount
        sql: base_buying_amount
        type: sum

      - name: deposit_amount
        sql: deposit_amount
        type: sum

      - name: rmt_balance
        sql: rmt_balance
        type: sum

      - name: tax_amount
        sql: tax_amount
        type: sum

      - name: requested_amount
        sql: requested_amount
        type: sum

      - name: master_card_fee_amount
        sql: master_card_fee_amount
        type: sum

      - name: balance_amount
        sql: balance_amount
        type: sum

      - name: billing_amount
        sql: billing_amount
        type: sum

      - name: total_amount
        sql: total_amount
        type: sum

      - name: base_amount
        sql: base_amount
        type: sum

      - name: fee_amount
        sql: fee_amount
        type: sum

      - name: parent_account
        sql: parent_account
        type: sum

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

