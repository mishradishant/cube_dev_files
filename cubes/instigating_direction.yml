cubes:
  - name: instigating_direction
    sql_table: DIM.vw_InstigatingDirection
    data_source: default

    joins: []

    dimensions:
      - name: InstigatingDirectionKey
        sql: InstigatingDirectionKey
        type: number
        primary_key: true

      - name: InstigatingDirectionType
        sql: InstigatingDirectionType
        type: string
   


    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started